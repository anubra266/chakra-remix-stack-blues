import { SystemStyleObject } from "@chakra-ui/react";

const baseStyle: SystemStyleObject = {
  cursor: "pointer",
  _hover: {
    textDecoration: "none",
  },
};

export const Link = {
  baseStyle,
};
